# Data Model: Fix Scoreboard Layout for 5-6 Players

**Feature**: `006-fix-scoreboard-layout`

## Status: N/A

This feature involves **pure CSS/layout changes only**. No data model modifications, new entities, state transitions, or storage schema changes are required.

- Existing `Player` type (`src/types/game.ts`) is unchanged.
- Existing `Game` type is unchanged.
- No new props added to `ScoreBoard`, `PinInput`, or `GameScreen` components beyond what already exists.
